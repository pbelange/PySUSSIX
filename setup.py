from setuptools import setup, find_packages

setup(
    name    ='PySUSSIX',
    version ='1.00',
    packages=find_packages(where='PySUSSIX'),  
    install_requires=[
        # List your dependencies here
    ],

)